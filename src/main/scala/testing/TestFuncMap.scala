package testing
//
import parser._
//
object TestFuncMap {
//    def run() = {
//        // test code
////        val m = Map("fire" -> fire _, "rapidFire" -> rapidFire _)
////
////        //rapidFire(1)
////        m("fire").asInstanceOf[() => Unit]()
////        m("rapidFire").asInstanceOf[(Int) => Unit](5)
////
////        val lit = List(IntLit(5), DoubleLit(.5))
////
////        val v0 = conv(lit(0)).asInstanceOf[ty(lit(0))]
////
////        curryFunc()(conv(lit(1)).asInstanceOf[ty(lit(1))])
////    }
//
//    def conv(literal: Literal) = {
//        literal match {
//            case IntTy(x) => x
//            case DoubleTy(x) => x
//        }
//    }
//
//    def ty(literal: Literal) = {
//        literal match {
//            case IntTy(_) => Int
//            case DoubleTy(_) => Double
//        }
//    }
//
//    def fire() = {
//        println("FIRE!")
//    }
//
//    def rapidFire(i:Int) = {
//        for (a <- 1 to i) {
//            println("FIRE!")
//        }
//    }
//
//    def curryFunc(i:Int)(d:Double) = {
//        for (a <- 1 to i) {
//            println(d)
//        }
//    }
//
//    def applyParameters() = Unit
}
